package com.enterprise.web.tests;

import com.enterprise.web.drivers.WebDriverFactory;
import org.testng.Assert;
import org.testng.annotations.Test;

public class ErrorTests extends BaseTest {

    @Test(description = "Auto-generated test TC_ERROR_001")
    public void TC_ERROR_001_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_001");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_002")
    public void TC_ERROR_002_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_002");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_003")
    public void TC_ERROR_003_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_003");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_004")
    public void TC_ERROR_004_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_004");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_005")
    public void TC_ERROR_005_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_005");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_006")
    public void TC_ERROR_006_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_006");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_007")
    public void TC_ERROR_007_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_007");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_008")
    public void TC_ERROR_008_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_008");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_009")
    public void TC_ERROR_009_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_009");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_010")
    public void TC_ERROR_010_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_010");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_011")
    public void TC_ERROR_011_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_011");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_012")
    public void TC_ERROR_012_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_012");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_013")
    public void TC_ERROR_013_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_013");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_014")
    public void TC_ERROR_014_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_014");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_015")
    public void TC_ERROR_015_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_015");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_016")
    public void TC_ERROR_016_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_016");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_017")
    public void TC_ERROR_017_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_017");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_018")
    public void TC_ERROR_018_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_018");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_019")
    public void TC_ERROR_019_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_019");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_ERROR_020")
    public void TC_ERROR_020_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_ERROR_020");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

}
