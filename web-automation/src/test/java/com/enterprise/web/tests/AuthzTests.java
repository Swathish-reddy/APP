package com.enterprise.web.tests;

import com.enterprise.web.drivers.WebDriverFactory;
import org.testng.Assert;
import org.testng.annotations.Test;

public class AuthzTests extends BaseTest {

    @Test(description = "Auto-generated test TC_AUTHZ_001")
    public void TC_AUTHZ_001_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_001");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_002")
    public void TC_AUTHZ_002_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_002");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_003")
    public void TC_AUTHZ_003_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_003");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_004")
    public void TC_AUTHZ_004_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_004");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_005")
    public void TC_AUTHZ_005_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_005");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_006")
    public void TC_AUTHZ_006_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_006");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_007")
    public void TC_AUTHZ_007_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_007");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_008")
    public void TC_AUTHZ_008_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_008");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_009")
    public void TC_AUTHZ_009_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_009");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_010")
    public void TC_AUTHZ_010_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_010");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_011")
    public void TC_AUTHZ_011_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_011");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_012")
    public void TC_AUTHZ_012_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_012");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_013")
    public void TC_AUTHZ_013_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_013");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_014")
    public void TC_AUTHZ_014_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_014");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_015")
    public void TC_AUTHZ_015_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_015");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_016")
    public void TC_AUTHZ_016_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_016");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_017")
    public void TC_AUTHZ_017_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_017");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_018")
    public void TC_AUTHZ_018_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_018");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_019")
    public void TC_AUTHZ_019_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_019");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_020")
    public void TC_AUTHZ_020_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_020");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_021")
    public void TC_AUTHZ_021_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_021");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_022")
    public void TC_AUTHZ_022_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_022");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_023")
    public void TC_AUTHZ_023_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_023");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_024")
    public void TC_AUTHZ_024_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_024");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_025")
    public void TC_AUTHZ_025_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_025");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_026")
    public void TC_AUTHZ_026_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_026");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_027")
    public void TC_AUTHZ_027_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_027");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_028")
    public void TC_AUTHZ_028_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_028");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_029")
    public void TC_AUTHZ_029_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_029");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_030")
    public void TC_AUTHZ_030_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_030");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_031")
    public void TC_AUTHZ_031_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_031");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_032")
    public void TC_AUTHZ_032_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_032");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_033")
    public void TC_AUTHZ_033_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_033");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_034")
    public void TC_AUTHZ_034_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_034");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_035")
    public void TC_AUTHZ_035_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_035");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_036")
    public void TC_AUTHZ_036_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_036");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_037")
    public void TC_AUTHZ_037_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_037");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_038")
    public void TC_AUTHZ_038_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_038");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_039")
    public void TC_AUTHZ_039_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_039");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_AUTHZ_040")
    public void TC_AUTHZ_040_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_AUTHZ_040");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

}
