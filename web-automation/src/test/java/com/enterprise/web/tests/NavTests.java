package com.enterprise.web.tests;

import com.enterprise.web.drivers.WebDriverFactory;
import org.testng.Assert;
import org.testng.annotations.Test;

public class NavTests extends BaseTest {

    @Test(description = "Auto-generated test TC_NAV_001")
    public void TC_NAV_001_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_001");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_002")
    public void TC_NAV_002_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_002");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_003")
    public void TC_NAV_003_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_003");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_004")
    public void TC_NAV_004_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_004");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_005")
    public void TC_NAV_005_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_005");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_006")
    public void TC_NAV_006_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_006");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_007")
    public void TC_NAV_007_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_007");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_008")
    public void TC_NAV_008_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_008");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_009")
    public void TC_NAV_009_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_009");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_010")
    public void TC_NAV_010_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_010");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_011")
    public void TC_NAV_011_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_011");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_012")
    public void TC_NAV_012_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_012");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_013")
    public void TC_NAV_013_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_013");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_014")
    public void TC_NAV_014_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_014");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_015")
    public void TC_NAV_015_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_015");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_016")
    public void TC_NAV_016_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_016");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_017")
    public void TC_NAV_017_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_017");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_018")
    public void TC_NAV_018_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_018");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_019")
    public void TC_NAV_019_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_019");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_020")
    public void TC_NAV_020_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_020");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_021")
    public void TC_NAV_021_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_021");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_022")
    public void TC_NAV_022_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_022");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_023")
    public void TC_NAV_023_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_023");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_024")
    public void TC_NAV_024_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_024");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_025")
    public void TC_NAV_025_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_025");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_026")
    public void TC_NAV_026_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_026");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_027")
    public void TC_NAV_027_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_027");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_028")
    public void TC_NAV_028_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_028");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_029")
    public void TC_NAV_029_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_029");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_NAV_030")
    public void TC_NAV_030_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_NAV_030");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

}
