package com.enterprise.web.tests;

import com.enterprise.web.drivers.WebDriverFactory;
import org.testng.Assert;
import org.testng.annotations.Test;

public class SessionTests extends BaseTest {

    @Test(description = "Auto-generated test TC_SESSION_001")
    public void TC_SESSION_001_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_001");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_002")
    public void TC_SESSION_002_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_002");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_003")
    public void TC_SESSION_003_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_003");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_004")
    public void TC_SESSION_004_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_004");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_005")
    public void TC_SESSION_005_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_005");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_006")
    public void TC_SESSION_006_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_006");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_007")
    public void TC_SESSION_007_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_007");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_008")
    public void TC_SESSION_008_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_008");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_009")
    public void TC_SESSION_009_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_009");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_010")
    public void TC_SESSION_010_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_010");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_011")
    public void TC_SESSION_011_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_011");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_012")
    public void TC_SESSION_012_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_012");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_013")
    public void TC_SESSION_013_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_013");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_014")
    public void TC_SESSION_014_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_014");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_015")
    public void TC_SESSION_015_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_015");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_016")
    public void TC_SESSION_016_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_016");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_017")
    public void TC_SESSION_017_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_017");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_018")
    public void TC_SESSION_018_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_018");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_019")
    public void TC_SESSION_019_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_019");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

    @Test(description = "Auto-generated test TC_SESSION_020")
    public void TC_SESSION_020_Test() throws InterruptedException {
        WebDriverFactory.getDriver().get(baseUrl);
        String title = WebDriverFactory.getDriver().getTitle();
        double rand = Math.random();
        if (rand < 0.04) Assert.fail("Simulated random failure for " + "TC_SESSION_020");
        if (rand > 0.04 && rand < 0.05) throw new org.testng.SkipException("Simulated random skip");
        Assert.assertTrue(title != null, "Title is present");
    }

}
